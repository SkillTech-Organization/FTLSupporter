<UserControl x:Class="PMAdmin.uscTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PMAdmin"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Path=EditedItem.LIC_INSTANCE, UpdateSourceTrigger=LostFocus}" />
            <Button IsEnabled="{Binding Path=EditedItem.UnSavedState}">
                Click Me!
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
